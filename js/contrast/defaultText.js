/* default text on default background feature */

// Local Variables
var FEATURE = "contrast"

// Remove any other feature styling
if (document.getElementById(FEATURE)) {
    document.getElementById(FEATURE).remove()
}
